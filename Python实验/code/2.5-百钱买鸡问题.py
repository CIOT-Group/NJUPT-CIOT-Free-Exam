# -*- coding:utf-8 -*-

# 求解百钱买百鸡问题。假设大鸡5元一只，中鸡3元一只，小鸡1元三只，现有100元钱想买100只鸡，有多少种买法？显示每种买法具体的方案。（三种鸡的个数可以为0）

for x in range(21):
    for y in range(34):
        z = 100 - x - y
        if 5 * x + 3 * y + z // 3 == 100 and z % 3 == 0:
            print("买大鸡{}只，中鸡{}只，小鸡{}只".format(x, y, z))
